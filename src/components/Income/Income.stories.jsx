import React from 'react';
import Income from './Income';

export default {
    title: 'Income',
    component: Income
}

const Template = (args) => <Income {...args}/>;

export const Default = Template.bind({});
